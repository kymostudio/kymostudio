# mermaid-cypress

> Inline diagram sources extracted from mermaid.js's own cypress rendering specs.

| | |
|---|---|
| Sources | 803 |
| Diagram types | 24 |
| Origin | [mermaid-js/mermaid](https://github.com/mermaid-js/mermaid) `cypress/integration/rendering/` (`imgSnapshotTest`/`renderGraph` literals) |
| License | MIT |
| Format | `<grammar>/<name>.mmd` — one file per diagram, split by type. |

## Diagram types

`architecture`, `block`, `c4`, `class`, `er`, `flowchart`, `gantt`, `gitgraph`, `info`, `journey`, `kanban`, `mindmap`, `packet`, `pie`, `quadrant`, `radar`, `requirement`, `sankey`, `sequence`, `state`, `timeline`, `treemap`, `xychart`, `zenuml`

## Use

Raw sources for render/convert **coverage** — no ground-truth labels, so not part of the scored run. Deduped by content against the other datasets.
