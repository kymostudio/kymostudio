# mermaid-to-svg

> Sample diagrams from Warp's pure-Rust mermaid→SVG converter — a sibling effort to kymo's own engine.

| | |
|---|---|
| Sources | 85 |
| Diagram types | 21 |
| Origin | [warpdotdev/mermaid-to-svg](https://github.com/warpdotdev/mermaid-to-svg) `samples/` |
| License | MIT |
| Format | `<grammar>/<name>.mmd` — one file per diagram, split by type. |

## Diagram types

`block`, `c4`, `class`, `er`, `flowchart`, `gantt`, `gitgraph`, `info`, `journey`, `kanban`, `mindmap`, `packet`, `pie`, `quadrant`, `radar`, `requirement`, `sankey`, `sequence`, `state`, `timeline`, `xychart`

## Use

Raw sources for render/convert **coverage** — no ground-truth labels, so not part of the scored run. Deduped by content against the other datasets.
