# Canvas Jam — Change-Request Log

Change-requests against the baselined `canvas-jam` spec. One file per CR
(`CR-JAM-NNN.md`); log each row in the table below.

| CR | Title | Target baseline | Status | Date |
|----|-------|-----------------|--------|------|
| _none yet_ | | | | |
