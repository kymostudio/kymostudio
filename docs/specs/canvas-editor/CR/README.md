# Interactive Canvas Editor — Change-Request Log

Change-requests against the baselined `canvas-editor` spec. One file per CR
(`CR-CANVAS-NNN.md`); log each row in the table below.

| CR | Title | Target baseline | Status | Date |
|----|-------|-----------------|--------|------|
| _none yet_ | | | | |
