# In-House Canvas Engine — Change-Request Log

Change-requests against the baselined `canvas-engine` spec. One file per CR
(`CR-ENGINE-NNN.md`); log each row in the table below.

| CR | Title | Target baseline | Status | Date |
|----|-------|-----------------|--------|------|
| _none yet_ | | | | |
