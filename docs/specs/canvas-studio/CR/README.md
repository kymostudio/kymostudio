# Canvas Studio — Change-Request Log

Change-requests against the baselined `canvas-studio` spec (`docs/specs/canvas-studio/`). One file per
CR (`CR-STUDIO-NNN.md`); log each row in the table below.

| CR | Title | Target baseline | Status | Date |
|----|-------|-----------------|--------|------|
| _none yet_ | | | | |
