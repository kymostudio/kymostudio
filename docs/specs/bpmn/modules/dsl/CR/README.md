# BPMN-in-DSL — Change-Request Log

Change-requests against the baselined `bpmn-dsl` spec. One file per CR
(`CR-BPMN-DSL-NNN.md`); log each row in the table below.

| CR | Title | Target baseline | Status | Date |
|----|-------|-----------------|--------|------|
| _none yet_ | | | | |
