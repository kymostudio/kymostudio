# BPMN 2.0 Export — Change-Request Log

Change-requests against the baselined `bpmn-export` spec. One file per CR
(`CR-BPMN-EXPORT-NNN.md`); log each row in the table below.

| CR | Title | Target baseline | Status | Date |
|----|-------|-----------------|--------|------|
| _none yet_ | | | | |
