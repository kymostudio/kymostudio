{
  "name": "kymostudio-playground",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Client-side React playground for the kymostudio DSL — bundled by esbuild into the committed kymo.bundle.js (Phase 0: parity port of the vanilla playground).",
  "scripts": {
    "build": "./build.sh",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.2.3",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
