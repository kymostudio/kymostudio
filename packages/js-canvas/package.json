{
  "name": "kymostudio-canvas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "In-house canvas engine for the kymostudio playground — the tldraw replacement (store, editor, viewport, shapes). Internal, not published; consumed by website/app via the engine adapter.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
