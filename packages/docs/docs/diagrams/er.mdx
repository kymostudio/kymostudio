---
title: Entity Relationship Diagram
outline: false
sidebar: false
---

import { DiagramQuickstart, DqSection } from "@components";

<DiagramQuickstart set="er">

# Entity Relationship Diagram

An entity relationship diagram describes a data model: the entities (things a
system stores) and the relationships between them, with cardinalities on each
end. kymo's editor reads the
[Mermaid](https://mermaid.js.org/syntax/entityRelationshipDiagram.html)
`erDiagram` syntax.

This page works like a quickstart: as you scroll, the pane on the right shows
the source and the preview for the section you're reading. **Copy** grabs the
source; **▶ Open in editor** loads it into
[editor.kymo.studio](https://editor.kymo.studio) (pick **mermaid** in the
diagram-type dropdown when starting from scratch).

<DqSection id="er-intro">

The example on the right is the classic order model: a statement is
`ENTITY <cardinality>--<cardinality> ENTITY : label`, and the label names the
relationship from the first entity's point of view.

</DqSection>

<DqSection id="er-cardinality">

## Cardinality

Each end of the relationship operator carries a two-character cardinality
marker, read **outward** from the dashes:

| Left | Right | Meaning |
|------|-------|---------|
| `\|o` | `o\|` | zero or one |
| `\|\|` | `\|\|` | exactly one |
| `}o` | `o{` | zero or more |
| `}\|` | `\|{` | one or more |

The dashes in the middle encode **identification**: `--` is an identifying
relationship (the child cannot exist without the parent — drawn solid), `..`
is non-identifying (drawn dashed).

</DqSection>

<DqSection id="er-attributes">

## Attributes

A `{ … }` block after an entity lists its attributes as `type name` pairs.
A trailing key keyword marks `PK` (primary key), `FK` (foreign key), or `UK`
(unique key) — combine them with commas (`PK, FK`) — and a trailing quoted
string adds a comment.

</DqSection>

> **Status.** ER diagram previews on this page and in the editor use the
> Mermaid renderer; importing ER diagrams into kymo's own pipeline (native
> SVG/PNG/PDF rendering) is on the roadmap.

## See also

- [Class Diagram](./class) — for modelling behaviourful objects rather than
  stored data.
- [Best Practices](./best-practices) — layout and readability guidance.

</DiagramQuickstart>
