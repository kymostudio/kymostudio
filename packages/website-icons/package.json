{
  "name": "@kymostudio/website-icons",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "kymo icons browser (icons.kymo.studio) — React app for searching the KymoStudio icon library. Icon art is served from Cloudflare R2 (cdn.kymo.studio); built by build.sh into dist/, deployed to Cloudflare Pages (kymo-icons).",
  "license": "Apache-2.0",
  "scripts": {
    "build": "./build.sh",
    "dev": "cd dist && python3 -m http.server 4399",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "esbuild": "^0.28.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
