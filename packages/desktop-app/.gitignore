# Electron / npm
node_modules/
dist/
out/

# Python virtualenv (holds the kymostudio-core layout engine) — recreate with:
#   python3 -m venv .venv && .venv/bin/pip install "kymostudio-core>=0.4,<0.5"
.venv/
